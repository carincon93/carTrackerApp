// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebase: {
		projectId: 'cartracker-172b0',
		appId: '1:490714200408:web:545b2405258604c4c08d2d',
		storageBucket: 'cartracker-172b0.appspot.com',
		locationId: 'us-central',
		apiKey: 'AIzaSyBWGBsTP2dd7Eb0tjyDeaNwAILKRYxUqX4',
		authDomain: 'cartracker-172b0.firebaseapp.com',
		messagingSenderId: '490714200408',
	},
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
